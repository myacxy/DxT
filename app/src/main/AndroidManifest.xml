<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.myacxy.ditch"
    >
    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >

        <service
            android:name="net.myacxy.ditch.TwitchExtension"
            android:icon="@android:drawable/sym_def_app_icon"
            android:label="@string/app_name"
            android:permission="com.google.android.apps.dashclock.permission.READ_EXTENSION_DATA"
            >

            <intent-filter>
                <action android:name="com.google.android.apps.dashclock.Extension" />
            </intent-filter>

            <meta-data
                android:name="protocolVersion"
                android:value="2"
                />

            <meta-data
                android:name="worldReadable"
                android:value="true"
                />

            <meta-data
                android:name="description"
                android:value="Description"
                />

            <meta-data
                android:name="settingsActivity"
                android:value=".SettingsActivity"
                />

        </service>

        <activity
            android:name=".views.activities.MainActivity"
            android:exported="true"
            />
        <!--
        <activity android:name=".TwitchActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <!--<activity -->
            <!--android:name="net.myacxy.ditch.MainDialogActivity"-->
            <!--android:theme="@style/Popup"-->
            <!--android:icon="@drawable/twitch_white"-->
            <!--android:exported="true"-->
            <!--/>-->

        <!--<activity -->
            <!--android:name="net.myacxy.ditch.TwitchSettingsActivity"-->
            <!--android:label="@string/settings_title"-->
            <!--android:exported="true"-->
            <!--/>-->

    </application>
</manifest>
