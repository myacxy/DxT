<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.myacxy.dashclock.twitch" >
    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"

        android:icon="@drawable/twitch_purple"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar" >

        <service android:name=".TwitchExtension"
                 android:icon="@drawable/twitch_purple"
                 android:label="@string/app_name"
                 android:permission="com.google.android.apps.dashclock.permission.READ_EXTENSION_DATA">
            <intent-filter>
                <action android:name="com.google.android.apps.dashclock.Extension"/>
            </intent-filter>
            <meta-data android:name="protocolVersion"
                       android:value="2" />
            <meta-data android:name="worldReadable"
                       android:value="true" />
            <meta-data android:name="description"
                       android:value="@string/extension_description" />
            <meta-data android:name="settingsActivity"
                       android:value=".TwitchSettingsActivity" />
        </service>
        <!--
        <activity android:name=".TwitchActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity android:name=".MainDialogActivity"
                  android:theme="@style/Popup"
                  android:icon="@drawable/twitch_white"
                  android:exported="true">
        </activity>
        <activity android:name=".TwitchSettingsActivity"
                  android:label="@string/settings_title"
                  android:exported="true"/>
    </application>

</manifest>
